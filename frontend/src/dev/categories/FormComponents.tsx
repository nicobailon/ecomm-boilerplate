import CategoryView from './CategoryView';

export default function FormComponents() {
  return (
    <CategoryView
      category="forms"
      title="Form Components"
      description="Form inputs, validation components, and form utilities"
    />
  );
}